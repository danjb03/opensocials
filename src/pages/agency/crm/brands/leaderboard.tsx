
import React from 'react';
import BrandLeaderboard from '@/pages/admin/crm/brands/leaderboard';

// Agency brand leaderboard - same as admin but scoped to agency's brands
const AgencyBrandLeaderboard = () => {
  return <BrandLeaderboard />;
};

export default AgencyBrandLeaderboard;
