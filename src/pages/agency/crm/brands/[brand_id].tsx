
import React from 'react';
import BrandDetail from '@/pages/admin/crm/brands/[brand_id]';

// Agency brand detail - same as admin but scoped to agency's brands
const AgencyBrandDetail = () => {
  return <BrandDetail />;
};

export default AgencyBrandDetail;
