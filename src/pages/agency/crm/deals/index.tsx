
import React from 'react';
import DealsPipeline from '@/pages/admin/crm/deals';

// Agency deals pipeline - same as admin but scoped to agency's deals
const AgencyDealsPipeline = () => {
  return <DealsPipeline />;
};

export default AgencyDealsPipeline;
