
import React from 'react';
import CreatorLeaderboard from '@/pages/admin/crm/creators/leaderboard';

// Agency creator leaderboard - same as admin but scoped to agency's creators
const AgencyCreatorLeaderboard = () => {
  return <CreatorLeaderboard />;
};

export default AgencyCreatorLeaderboard;
