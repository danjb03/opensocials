
import React from 'react';
import CreatorDetail from '@/pages/admin/crm/creators/[id]';

// Agency creator detail - same as admin but scoped to agency's creators
const AgencyCreatorDetail = () => {
  return <CreatorDetail />;
};

export default AgencyCreatorDetail;
